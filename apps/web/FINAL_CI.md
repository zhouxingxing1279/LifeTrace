# Final Web redesign verification

This marker exists only to force the post-legacy-deletion Web verification workflows to run against the final repository tree.

Required final gates before merging PR #97:

- Web v2
- Browser Web
- Web Container Image

The production browser client is `apps/web`; the legacy `apps/desktop/web-client` directory has been removed.
