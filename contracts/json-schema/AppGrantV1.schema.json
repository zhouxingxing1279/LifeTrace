{
  "$comment": "GENERATED FILE - DO NOT EDIT MANUALLY. Source: crates/lifetrace-contracts.",
  "$defs": {
    "AppGrantId": {
      "description": "User-to-application grant identifier.",
      "type": "string"
    },
    "AppId": {
      "description": "Client application id. Known values: lifetrace-desktop, lifetrace-web, lifetrace-*-android.",
      "type": "string"
    },
    "Scope": {
      "description": "One OAuth-style authorization scope.",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "appId": {
      "$ref": "#/$defs/AppId"
    },
    "grantedAt": {
      "format": "date-time",
      "type": "string"
    },
    "id": {
      "$ref": "#/$defs/AppGrantId"
    },
    "revokedAt": {
      "format": "date-time",
      "type": [
        "string",
        "null"
      ]
    },
    "scopes": {
      "items": {
        "$ref": "#/$defs/Scope"
      },
      "type": "array"
    },
    "status": {
      "type": "string"
    },
    "updatedAt": {
      "format": "date-time",
      "type": "string"
    }
  },
  "required": [
    "id",
    "appId",
    "scopes",
    "status",
    "grantedAt",
    "updatedAt"
  ],
  "title": "AppGrantV1",
  "type": "object"
}
