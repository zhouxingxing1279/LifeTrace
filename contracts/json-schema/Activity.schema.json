{
  "$comment": "GENERATED FILE - DO NOT EDIT MANUALLY. Source: crates/lifetrace-contracts.",
  "$defs": {
    "ActivityCheckinMethod": {
      "description": "Habit check-in method. Known values: manual, automatic. Unknown values are preserved (forward compatible).",
      "type": "string"
    },
    "ActivityScheduleType": {
      "description": "Habit schedule type. Known values: daily, weekly, custom. Unknown values are preserved (forward compatible).",
      "type": "string"
    },
    "ActivitySyncSource": {
      "description": "Habit sync source. Known values: fitness, english. Unknown values are preserved (forward compatible).",
      "type": "string"
    },
    "ActivityType": {
      "description": "Habit activity type. Known values: duration, count, completion, weekly, control. Unknown values are preserved (forward compatible).",
      "type": "string"
    },
    "DeviceId": {
      "description": "Stable device identifier (string, usually UUID v4).",
      "type": "string"
    },
    "EntityId": {
      "description": "Entity identifier. New IDs are UUID v4; legacy non-UUID IDs are preserved verbatim.",
      "type": "string"
    },
    "EntityMeta": {
      "description": "Metadata shared by every syncable entity payload.\n\n`local_version` is the local revision counter (EPIC-01 `version` column).\nIt is NOT a server authority; `server_version` is assigned by the server\nand `base_server_version` (on changes) is what the client knew when it\nmade the change. Offline edits must never fabricate `server_version`.",
      "properties": {
        "createdAt": {
          "format": "date-time",
          "type": "string"
        },
        "deletedAt": {
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "$ref": "#/$defs/EntityId"
        },
        "localVersion": {
          "description": "Local revision number (never the server-authoritative version).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "modifiedByDevice": {
          "anyOf": [
            {
              "$ref": "#/$defs/DeviceId"
            },
            {
              "type": "null"
            }
          ],
          "description": "Device that last modified the entity locally (audit only)."
        },
        "serverVersion": {
          "anyOf": [
            {
              "$ref": "#/$defs/ServerVersion"
            },
            {
              "type": "null"
            }
          ],
          "description": "Server-authoritative version; `None` until the server first accepts it."
        },
        "updatedAt": {
          "format": "date-time",
          "type": "string"
        },
        "userId": {
          "$ref": "#/$defs/UserId"
        }
      },
      "required": [
        "id",
        "userId",
        "createdAt",
        "updatedAt",
        "localVersion"
      ],
      "type": "object"
    },
    "LocalDate": {
      "description": "A natural calendar day in `YYYY-MM-DD` (wire format).",
      "type": "string"
    },
    "ServerVersion": {
      "description": "Server-authoritative entity version. Wire format: decimal string\n(for example `\"42\"`) to avoid JavaScript safe-integer issues.",
      "type": "string"
    },
    "UserId": {
      "description": "Stable user identifier (string, usually UUID v4).",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "`habit.activity`",
  "properties": {
    "activityType": {
      "$ref": "#/$defs/ActivityType"
    },
    "checkinMethod": {
      "anyOf": [
        {
          "$ref": "#/$defs/ActivityCheckinMethod"
        },
        {
          "type": "null"
        }
      ]
    },
    "color": {
      "type": [
        "string",
        "null"
      ]
    },
    "description": {
      "type": [
        "string",
        "null"
      ]
    },
    "icon": {
      "type": [
        "string",
        "null"
      ]
    },
    "isArchived": {
      "type": "boolean"
    },
    "meta": {
      "$ref": "#/$defs/EntityMeta"
    },
    "minimumTarget": {
      "format": "double",
      "type": [
        "number",
        "null"
      ]
    },
    "name": {
      "type": "string"
    },
    "normalTarget": {
      "format": "double",
      "type": [
        "number",
        "null"
      ]
    },
    "scheduleType": {
      "anyOf": [
        {
          "$ref": "#/$defs/ActivityScheduleType"
        },
        {
          "type": "null"
        }
      ]
    },
    "startDate": {
      "anyOf": [
        {
          "$ref": "#/$defs/LocalDate"
        },
        {
          "type": "null"
        }
      ]
    },
    "syncSource": {
      "anyOf": [
        {
          "$ref": "#/$defs/ActivitySyncSource"
        },
        {
          "type": "null"
        }
      ]
    },
    "targetDays": {
      "items": {
        "format": "uint8",
        "maximum": 255,
        "minimum": 0,
        "type": "integer"
      },
      "type": "array"
    },
    "targetPeriod": {
      "type": "string"
    },
    "unit": {
      "type": "string"
    }
  },
  "required": [
    "meta",
    "name",
    "activityType",
    "unit",
    "targetPeriod",
    "targetDays",
    "isArchived"
  ],
  "title": "Activity",
  "type": "object"
}
