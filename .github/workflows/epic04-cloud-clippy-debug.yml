name: EPIC-04 Cloud Clippy Diagnostic
# diagnostic-revision: 2

on:
  push:
    branches:
      - agent/epic-04-auth-complete
    paths:
      - '.github/workflows/epic04-cloud-clippy-debug.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply EPIC-04
        shell: bash
        run: |
          python - <<'PY'
          import base64, gzip
          from pathlib import Path
          payload = ''.join(path.read_text() for path in sorted(Path('.epic04').glob('patch-*')))
          Path('/tmp/epic04.patch').write_bytes(gzip.decompress(base64.b64decode(payload)))
          PY
          git apply /tmp/epic04.patch
          python .epic04/apply-fix.py
          python .epic04/apply-contract-fix.py
          python .epic04/apply-clippy-fix.py
          python .epic04/apply-cloud-clippy-fix.py
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.88.0
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            services/lifetrace-cloud
            crates/lifetrace-contracts
          shared-key: epic04-cloud-clippy-diagnostic
      - name: Format cloud
        run: cargo fmt --manifest-path services/lifetrace-cloud/Cargo.toml
      - name: Run and summarize Clippy
        shell: bash
        run: |
          set +e
          cargo clippy --manifest-path services/lifetrace-cloud/Cargo.toml --all-targets -- -D warnings > /tmp/clippy.log 2>&1
          status=$?
          perl -pe 's/\e\[[0-9;]*[mK]//g' /tmp/clippy.log > /tmp/clean.log
          {
            echo "exit_status=$status"
            grep -n -A24 -B12 -E '^error:|warning:|clippy::|help:|--> .*\.rs:' /tmp/clean.log | tail -1200 || true
            echo "=== LOG TAIL ==="
            tail -450 /tmp/clean.log
          } > epic04-cloud-clippy-summary.txt
          cat epic04-cloud-clippy-summary.txt
          exit $status
      - name: Upload concise diagnostic
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: epic04-cloud-clippy-summary
          path: epic04-cloud-clippy-summary.txt
          if-no-files-found: error
