
# LifeTrace UI 重构实施记录

> 实施日期：2026-08-07  
> 对应方案：`docs/ui-redesign/EXECUTION_PLAN.md`

## 已完成

- 建立统一字体、间距、颜色、圆角、控件高度和层级 Token。
- 将常规正文提升到 `15px / 22px`，次要信息提升到 `14px / 20px`。
- 收紧页面标题文案，减少常驻解释性小字。
- 建立可键盘操作、自动避让窗口边缘的统一 Action 菜单。
- 建立右键菜单和 `···` 菜单共用的 `AppAction` 数据模型。
- 建立统一危险操作确认弹窗。
- 为全局页面增加右键菜单和对应的顶部 `···` 入口。
- 将坚持项目从卡片墙改为可扫描列表，并接入对象级右键菜单。
- 将账单行接入右键菜单，收纳笔记、复制、编辑和删除操作。
- 将账户改为列表布局，并接入右键菜单和危险操作确认。
- 将笔记编辑器原有独立更多菜单迁移到统一 Action 菜单。
- 增加舒适 / 紧凑两档密度设置并保存到本地偏好。
- 增加菜单定位与 Action 解析单元测试。

## 兼容性约束

- 输入框、下拉框、文本域、Tiptap 编辑器继续使用系统原生文本右键菜单。
- 右键菜单中的重要操作均可通过页面主按钮或 `···` 菜单访问。
- 未修改业务数据模型、SQLite 结构和 Tauri command。
- 未引入 Electron 专有 API。

## 验证命令

```bash
npm run lint
npm run test:unit
npm run web:build
npm run browser:build
npm test
npm run test:rust
```
